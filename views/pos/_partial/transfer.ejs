<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta lang="en">

        </head>

<body>

<div class="alert alert-info">
Please select who will prepare order no : <%- order.oid %>.
</div>

<div class="row align-items-center g-2">
<div class="col-auto">
<label for="transfer"><b> Transfer To : </b></label>
</div>
<div class="col-auto">
<select class="form-select" name="transfer" id="transfer">
<option disabled selected> Transfer to </option>
<% user.forEach((user)=> {%>
<% if (user.full == currentUser.full ) {%>
<% } else { %>
<option value="<%- user._id %>"><%- user.full %></option>
<% } %>
<% }) %>
</select>
</div>
<div class="col-auto">
<button type="button" class="btn btn-outline-success"> Transfer </button>
</div>
</div>